{
  "name": "glassline-ui-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Open real-time HTML/CSS/JS UI middleware for games.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm --workspace packages/glassline-runtime run build",
    "typecheck": "npm --workspace packages/glassline-runtime run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
